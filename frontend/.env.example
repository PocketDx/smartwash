# Copiar a .env.local  ->  cp .env.example .env.local
# URL del backend Django. Solo se usa en el servidor de Next.js (rewrite y
# Server Components); el navegador siempre pide a /api/... del propio origen.
BACKEND_URL=http://127.0.0.1:8000
